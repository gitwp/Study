一.泛型类
1、泛型类的写法：尖括号E--<E>，跟在类名后面
2、继承泛型类时必须明确父类的泛型的类型，如果忽略则默认泛型类型为Object,如果不写沿用父类的泛型则编译器会报错
3、不存在泛型类

二、泛型接口
1、泛型接口继承时、如果不具体话泛型参数，那么继承类必须带有父类的泛型

三、类型通配符?
1、集合类型List<?>可表示多种类型
2、类型通配符上限下限：extends super用法

四、泛型方法
1、泛型方法的定义是
 修饰符 <T, S> 返回值类型 方法名(形参列表){
 	//方法体...
 }