桥梁模式
1、桥梁模式是对象的结构模式，所谓桥梁模式只的是类之间关系结构的一种抽象，类似于一座桥梁
2、核心思想：将抽象化(Abstraction)与实现化(Implementation)脱耦，使得二者可以独立地变化
①、实现化是具体要做的事情、它可能包含多个维度上的变化
②、实现化中的每个一个维度的抽象
③、桥梁的体现：具体实现类通过接口将相关的依赖维度（抽象的实现）注入到其中，
      实现桥梁的作用，因为桥上可以通过不同的抽象维度的实现

实例：
如果一个实现化包含两个维度上的变化，每个维度又有N和M个具体的实现，在没有将这两个维度分离出来的情况下
那么实现化的组合结果共计N*M个。分离出来中后对应着N+M+2（抽象化维度的父类）个实现
