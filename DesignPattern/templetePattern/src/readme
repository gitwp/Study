模板模式是基于继承的代码复用技术
模板：规则框架的制定者、是抽象的。

模板中通常包含2大类对象：
1、模板对象(包含模板的各个细节 public 也是对外暴露的对象)
2、模板细节对象
	①、必填项：具体的已经定义明确的对象(private final)
	②、自由发挥项：抽象的对象(protected abstract)
	③、可选项：可选可不选的对象(protected 空方法也叫钩子方法，名字应当以do开始，这是熟悉设计模式的Java开发人员的标准做法。)
	
常见应用场景：
1、Servlet的实现：一般具体的Servlet继承HttpServlet(抽象模板)，实现doGet()、doPost()方法，模板方法service()接受web服务器封装后的http请求，后调用具体的doXXX方法处理